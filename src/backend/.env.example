# Server Configuration
NODE_ENV=development
PORT=5000
API_VERSION=v1

# MongoDB Configuration
MONGODB_URI=mongodb+srv://karrichanikya_db_user:chanikya%40006@uniserve.saedfzj.mongodb.net/crbs_db
# Note: Special characters in password must be URL-encoded (@ becomes %40)

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRE=24h
JWT_REFRESH_EXPIRE=7d

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@example.com
EMAIL_PASSWORD=your_email_password
EMAIL_FROM=noreply@uniserve.edu

# Application Settings
MAX_BOOKING_DAYS_ADVANCE=90
BOOKING_TIMEOUT_MINUTES=30
SESSION_TIMEOUT_HOURS=24
MAX_LOGIN_ATTEMPTS=5

# Notification Settings
ENABLE_EMAIL_NOTIFICATIONS=true
REMINDER_HOURS_BEFORE=24

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
